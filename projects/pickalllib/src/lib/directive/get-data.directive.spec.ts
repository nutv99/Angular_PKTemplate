import { GetDataDirective } from './get-data.directive';

describe('GetDataDirective', () => {
  it('should create an instance', () => {
    const directive = new GetDataDirective();
    expect(directive).toBeTruthy();
  });
});
